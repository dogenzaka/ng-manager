#entity-table-head
  table.head
    thead
      tr
        th.edit
        th(ng-repeat='field in fields' ng-style='field.style' head-cell)
        th.remove
#entity-table-body(infinite-scroll='loadMore()' infinite-scroll-distance='0')
  table.body
    tbody
      tr(ng-repeat='row in rows' entity-row='' fields='fields' row='row' kind='kind')

